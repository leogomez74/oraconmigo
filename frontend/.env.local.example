## Backend base URL used by frontend fetch helpers (see `frontend/lib/auth.ts`).
## Set to empty string to use Next.js rewrites/proxy on the same origin.
NEXT_PUBLIC_API_URL=http://localhost:8000

## Backend base URL used by Next.js rewrites (see `frontend/next.config.ts`).
## In production this must be a PUBLIC URL (not localhost), e.g. https://api.oraconmigo.app
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000
